# Runtime dependencies for the Model Drift Watchdog.
openai>=1.40      # OpenAI-compatible providers: Groq, OpenRouter, GitHub Models
jsonschema>=4.20  # json_schema checker
requests>=2.31    # Gemini REST + Discord webhook
PyYAML>=6.0       # config.yml

